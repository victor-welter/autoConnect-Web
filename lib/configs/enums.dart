enum LancamentoType { insert, update }

enum LancamentoErrorType { error, alert }

///Enum para deixar a tela de /selecionar-registro dinâmica e identificar cada tipo de seleção que deve ser feita
enum SelectDataType {
  categoria,
  locais,
  marca,
  modelo,
  veiculo,
  tipoCombustivel,
  tipoDespesa,
  tipoProblema,
  notificacao,
}
